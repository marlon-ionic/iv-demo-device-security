import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'iv-demo-device-security',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
